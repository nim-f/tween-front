import React from 'react';
import TextField from '../text_field';

export default function SearchField() {
  return (
    <div>
      <TextField
        placeholder="Filter a person"
      />
    </div>
  );
}
